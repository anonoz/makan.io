.row
  table[valign="top"]
    thead
      th ID
      th[width=500] Customer Info
      th[width=500] Items
      th[width=200] $$
      th Status
      th
    tbody
      - @order_chits.each do |chit|
        == render "chit", chit: chit

.row
  = will_paginate @order_chits
